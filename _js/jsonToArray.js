const jsonToArray = (entities) => {
  let ret = []
  
  entities.entries.map((e) => {
    [[[k1], [v1]], [[k], [v]]]
    
    
    
    
    
    // [k1 : v1]

    if (typeof (e[1] === Object))

    let ret = `${e[0]} : ${e1}`
    // e[0] : e[1]
  })
}